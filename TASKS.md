# Phase 1: START (Project Analysis)
- [ ] Verify current structure and functionality.
- [ ] Install dependencies (`npm install`).
- [ ] Identify areas missing modular separation.

# Phase 2: PLAN (Modular Separation & Roo Code Directive)
- [ ] Clean directory structure.
- [ ] Separate `server.ts` logic into `api/` and `controllers/` as needed.
- [ ] Set up Vercel environment configurations.

# Phase 3: BUILD (Implement Features & Animations)
- [ ] Implement missing features for robust functionality.
- [ ] Add high-end smooth transitions using Framer Motion.
- [ ] Ensure pixel-to-pixel perfection with refined CSS (Tailwind).

# Phase 4: VERIFY (CodeRabbit & Final Checks)
- [ ] Perform pre-commit code review for bad patterns and unoptimized renders.
- [ ] Fix any build warnings and Next.js/Vite optimizations.
- [ ] Build locally (`npm run build`) and self-heal if builds fail.
